CLass Rectangle {
    int topLeft;
    int buttonRight;

    Rectangle(this.topLeft, this.buttonRight);

    int area((int topLeft, int buttonRight) => topLeft * buttonRight);
    int width ((int ))
        
}